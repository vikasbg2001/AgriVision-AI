from ultralytics import YOLO

# Load a pretrained YOLOv8 model
model = YOLO("yolov8n.pt")

# Train on your dataset
model.train(
    data="datasets/crop/data.yaml",
    epochs=100,
    imgsz=640,
    batch=16,
    project="runs",
    name="crop_model"
)

print("Training completed.")
